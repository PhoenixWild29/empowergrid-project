diff --git a/node_modules/bigint-buffer/src/index.ts b/node_modules/bigint-buffer/src/index.ts
index 3c78463..ad5a91c 100644
--- a/node_modules/bigint-buffer/src/index.ts
+++ b/node_modules/bigint-buffer/src/index.ts
@@ -23,6 +23,15 @@ if (!process.browser) {
  * @returns A BigInt with the little-endian representation of buf.
  */
 export function toBigIntLE(buf: Buffer): bigint {
+  // Security fix for CVE-2025-3194: Add input validation to prevent buffer overflow
+  if (!buf || !Buffer.isBuffer(buf)) {
+    throw new Error('Invalid buffer: must be a Buffer instance');
+  }
+  // Limit buffer size to prevent overflow (64KB max for safety)
+  const MAX_BUFFER_SIZE = 65536;
+  if (buf.length > MAX_BUFFER_SIZE) {
+    throw new Error(`Buffer size ${buf.length} exceeds maximum allowed size ${MAX_BUFFER_SIZE}`);
+  }
   if (process.browser || converter === undefined) {
     const reversed = Buffer.from(buf);
     reversed.reverse();
@@ -41,6 +50,15 @@ export function toBigIntLE(buf: Buffer): bigint {
  * @returns A BigInt with the big-endian representation of buf.
  */
 export function toBigIntBE(buf: Buffer): bigint {
+  // Security fix for CVE-2025-3194: Add input validation to prevent buffer overflow
+  if (!buf || !Buffer.isBuffer(buf)) {
+    throw new Error('Invalid buffer: must be a Buffer instance');
+  }
+  // Limit buffer size to prevent overflow (64KB max for safety)
+  const MAX_BUFFER_SIZE = 65536;
+  if (buf.length > MAX_BUFFER_SIZE) {
+    throw new Error(`Buffer size ${buf.length} exceeds maximum allowed size ${MAX_BUFFER_SIZE}`);
+  }
   if (process.browser || converter === undefined) {
     const hex = buf.toString('hex');
     if (hex.length === 0) {
